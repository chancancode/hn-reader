<h2>Newest</h2>

<ol {{bind-attr start=start}}>
  {{#each item in items}}
  <li class="item">
    {{#if item.tag}} <span class="tag">{{item.tag}}</span> {{/if}}
    <span class="title">{{item.title}}</span>
  </li>
  {{/each}}
</ol>

{{#if prev}} {{link-to "Prev" (query-params since=prev.id start=prevStart) rel="prev"}} {{/if}}

{{#if next}} {{link-to "Next" (query-params since=next.id start=nextStart) rel="next"}} {{/if}}